import { React } from "react";
import RightIcon from "../icons/RightIcon";

export default function SlideNextButton({clx}) {
  return (
    <button className={clx} >
      <RightIcon />
     </button>
  );
}
